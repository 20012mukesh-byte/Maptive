export async function savePdf(fileName: string, content: string, isValid: boolean) {
  console.log('📝 [mockDb] saving pdf:', fileName, content.length, isValid);
  // No-op for demo
}

export async function saveLayout() {
  console.log('💾 [mockDb] saving layout...');
  // No-op for demo
}
